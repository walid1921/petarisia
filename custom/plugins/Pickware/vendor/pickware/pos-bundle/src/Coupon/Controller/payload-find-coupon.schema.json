{
    "$schema": "http://json-schema.org/draft-07/schema",
    "description": "POST request payload for API route _action/pickware-pos/find-coupon",
    "type": "object",
    "required": ["code", "currencyId"],
    "properties": {
        "currencyId": {
            "$ref": "#/definitions/uuid"
        },
        "code": {
            "type": "string"
        }
    },
    "definitions": {
        "uuid": {
            "description": "A UUID in HEX format",
            "type": "string",
            "minLength": 32,
            "maxLength": 32,
            "pattern": "^[0-9a-f]{32}$"
        }
    }
}
