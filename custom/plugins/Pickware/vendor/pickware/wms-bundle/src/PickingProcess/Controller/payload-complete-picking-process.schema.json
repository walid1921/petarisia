{
    "$schema": "http://json-schema.org/draft-07/schema",
    "title": "Picking process complete-picking-process payload",
    "description": "POST request payload for api route _action/pickware-wms/complete-picking-process",
    "type": "object",
    "required": ["pickingProcessId", "associations"],
    "properties": {
        "pickingProcessId": {
            "$ref": "#/definitions/uuid"
        },
        "associations": {
            "type": "object"
        }
    },
    "definitions": {
        "uuid": {
            "description": "A UUID in HEX format",
            "type": "string",
            "minLength": 32,
            "maxLength": 32,
            "pattern": "^[0-9a-f]{32}$"
        }
    }
}
