{
    "$schema": "http://json-schema.org/draft-07/schema",
    "title": "Picking process take-over-picking-process payload",
    "description": "POST request payload for api route _action/pickware-wms/take-over-picking-process",
    "type": "object",
    "required": ["associations", "pickingProcessId"],
    "properties": {
        "associations": {
            "type": "object"
        },
        "pickingProcessId": {
            "$ref": "#/definitions/uuid"
        },
        "warehouseId": {
            "$ref": "#/definitions/uuid"
        }
    },
    "definitions": {
        "uuid": {
            "description": "A UUID in HEX format",
            "type": "string",
            "minLength": 32,
            "maxLength": 32,
            "pattern": "^[0-9a-f]{32}$"
        }
    }
}
