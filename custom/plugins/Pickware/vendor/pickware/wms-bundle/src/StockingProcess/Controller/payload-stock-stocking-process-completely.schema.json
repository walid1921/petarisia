{
    "$schema": "http://json-schema.org/draft-07/schema",
    "title": "Stocking process stock-stocking-process-completely payload",
    "description": "PUT request payload for api route _action/pickware-wms/stock-stocking-process-completely",
    "type": "object",
    "required": ["stockingProcessId", "associations"],
    "properties": {
        "stockingProcessId": {
            "$ref": "#/definitions/uuid"
        },
        "associations": {
            "type": "object"
        }
    },
    "definitions": {
        "uuid": {
            "description": "A UUID in HEX format",
            "type": "string",
            "minLength": 32,
            "maxLength": 32,
            "pattern": "^[0-9a-f]{32}$"
        }
    }
}
