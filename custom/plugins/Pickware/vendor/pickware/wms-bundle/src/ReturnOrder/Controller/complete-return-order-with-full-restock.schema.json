{
    "$schema": "http://json-schema.org/draft-07/schema",
    "title": "Create return orders",
    "description": "POST request payload for api route _action/pickware-wms/complete-return-order-with-full-restock",
    "type": "object",
    "required": ["returnOrderId", "warehouseId"],
    "properties": {
        "associations": {
            "type": "object"
        },
        "returnOrderId": {
            "$ref": "#/definitions/uuid"
        },
        "warehouseId": {
            "$ref": "#/definitions/uuid"
        }
    },
    "definitions": {
        "uuid": {
            "description": "A UUID in HEX format",
            "type": "string",
            "minLength": 32,
            "maxLength": 32,
            "pattern": "^[0-9a-f]{32}$"
        }
    }
}
