{
    "$schema": "http://json-schema.org/draft-07/schema",
    "title": "Return order receive-return-order payload",
    "description": "POST request payload for api route _action/pickware-erp/receive-return-order",
    "type": "object",
    "required": ["returnOrderId", "associations"],
    "additionalProperties": false,
    "properties": {
        "returnOrderId": {
            "$ref": "#/definitions/uuid"
        },
        "associations": {
            "type": "object"
        }
    },
    "definitions": {
        "uuid": {
            "description": "A UUID in HEX format",
            "type": "string",
            "minLength": 32,
            "maxLength": 32,
            "pattern": "^[0-9a-f]{32}$"
        }
    }
}
